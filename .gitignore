node_modules
dist
dist-ssr
*.local
scripts/.out.*.mjs

# Markdown docs are kept local only — not pushed.
*.md

# Local Claude session artifacts (scheduling lock/state)
.claude/scheduled_tasks.json
.claude/scheduled_tasks.lock

.DS_Store
.vscode/*
!.vscode/extensions.json
